# Cloudflare Account ID (found in Cloudflare dashboard > any domain > right sidebar)
CF_ACCOUNT_ID=""

# Cloudflare API Token (create at: My Profile > API Tokens > Custom Token)
# Required permission: Account | Account Firewall Access Rules | Edit
CF_API_TOKEN=""

# Your home IP to prevent self-lockout (leave blank if not needed)
MY_IP=""
